# Typst to Obsidian
This is a command-line Typst to Obsididan converter with Typst's native parser written in Rust and a converter written in Python.

## Usage
`sh convert.sh filename.typ`

## Setup
1. `pip install -r converter/requirements.txt`
